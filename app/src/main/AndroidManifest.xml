<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edbinns.poesiadelamusica">

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@drawable/poetry"
        android:label="@string/app_name"
        android:roundIcon="@drawable/poetry"
        android:supportsRtl="true"
        android:theme="@style/Theme.PoesiaDeLaMusica">
        <activity android:name=".view.activitys.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activitys.MainActivity">

        </activity>
    </application>

</manifest>